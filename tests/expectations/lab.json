{
  "_comment": "Expected status of every check, compared by `conformance --expect`. Any difference in either direction is an error: a check that silently degrades to \"skip\" leaves the score at 100% because skips are excluded from the denominator, which is this suite's dominant failure mode. Regenerate deliberately with `--write-expect`, and read the diff.",
  "checks": {
    "control.capver.minimum": "pass",
    "control.controlbase.framing": "pass",
    "control.hostinfo": "pass",
    "control.key.capver": "pass",
    "control.key.fetch": "pass",
    "control.noise.handshake": "pass",
    "control.reauth": "pass",
    "control.register": "pass",
    "derp.map.parse": "pass",
    "derp.relay": "pass",
    "disco.endpoints": "pass",
    "disco.ping": "pass",
    "disco.pong": "pass",
    "exit.advertise": "pass",
    "exit.forward.tcp": "external",
    "exit.forward.udp": "external",
    "keys.disco": "pass",
    "keys.encoding": "pass",
    "keys.machine": "pass",
    "keys.node": "pass",
    "keys.persistence": "pass",
    "netmap.compression": "pass",
    "netmap.delta": "pass",
    "netmap.fields": "pass",
    "netmap.streaming": "pass",
    "netmap.to_peers": "pass",
    "transport.http2": "pass",
    "transport.tls": "pass",
    "wg.handshake.initiator": "external",
    "wg.handshake.responder": "external",
    "wg.multipeer": "pass",
    "wg.rekey": "external",
    "wg.replay": "external",
    "wg.transport.roundtrip": "external"
  },
  "target": "Headscale (local lab)"
}
