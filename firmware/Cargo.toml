# Deliberately its own workspace: `.cargo/config.toml` next to this file pins
# the riscv target for every invocation in this directory, which cargo would
# otherwise apply to the whole root workspace and its host-side tests.
[workspace]

[package]
edition      = "2024"
name         = "esp-gateway-firmware"
rust-version = "1.88"
version      = "0.1.0"

[[bin]]
name = "esp-gateway-firmware"
path = "./src/main.rs"

[dependencies]
wg-core = { path = "../crates/wg-core" }

esp-hal = { version = "~1.1.0", features = ["esp32c6", "log-04", "unstable"] }

esp-rtos = { version = "0.3.0", features = [
  "embassy",
  "esp-alloc",
  "esp-radio",
  "esp32c6",
  "log-04",
] }

esp-bootloader-esp-idf = { version = "0.5.0", features = ["esp32c6", "log-04"] }
log                    = "0.4.27"

embassy-net = { version = "0.9.1", features = [
  # smoltcp does not answer pings unless this is on, and being able to ping the
  # gateway's LAN address is the first thing anyone reaches for when the tunnel
  # misbehaves.
  "auto-icmp-echo-reply",
  "dhcpv4",
  "log",
  "medium-ethernet",
  "udp",
] }
esp-alloc = "0.10.0"
esp-backtrace = { version = "0.19.0", features = [
  "esp32c6",
  "panic-handler",
  "println",
] }
esp-println = { version = "0.17.0", features = ["esp32c6", "log-04"] }
embassy-futures = "0.1"
embassy-executor = { version = "0.10.0", features = ["log"] }
embassy-time = { version = "0.5.0", features = ["log"] }
esp-radio = { version = "0.18.0", features = [
  "esp-alloc",
  "esp32c6",
  "log-04",
  "unstable",
  "wifi",
] }
smoltcp = { version = "0.13.0", default-features = false, features = [
  "log",
  "medium-ethernet",
  "proto-dhcpv4",
  "proto-ipv4",
  "socket-udp",
] }

critical-section = "1.2.0"
heapless         = "0.9"
static_cell      = "2.1.1"

# ChaCha20-Poly1305 is the throughput bottleneck of the tunnel and is unusably
# slow when compiled without optimization, so optimize dependencies even in
# debug builds.
[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = "s"

[profile.release]
codegen-units = 1
debug         = 2
lto           = "fat"
opt-level     = 3
